# Add project specific ProGuard rules here.
# Milestone 1 ships debug-only builds (isMinifyEnabled = false), so this
# file is not exercised yet. Kept in place for when release builds are
# introduced (e.g. once a signing config exists for distributing outside
# of CI-produced debug artifacts).
